[project]
name = "ask-anything"
version = "0.1.0"
description = "Internet Archive astronaut interview and Q&A video harvester"
requires-python = ">=3.11"
dependencies = [
  "requests>=2.32.0",
]

[tool.uv]
package = false
